<%= form_with(model: merchant_role, local: true) do |form| %>
  <% if merchant_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(merchant_role.errors.count, "error") %> prohibited this merchant_role from being saved:</h2>

      <ul>
      <% merchant_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :merchant_role_name %>
  </div>

  <div class="field">
    <%= form.label :merchant_id %>
    <%= form.number_field :merchant_id, id: :merchant_role_merchant_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
