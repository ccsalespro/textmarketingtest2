<%= form_with(model: company_plan, local: true) do |form| %>
  <% if company_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_plan.errors.count, "error") %> prohibited this company_plan from being saved:</h2>

      <ul>
      <% company_plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>