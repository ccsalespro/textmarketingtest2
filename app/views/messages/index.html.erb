<div style="padding-top: 20px;"></div>
<div class="col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
  <div class="well">

    <h1  class="text-center">Sent Messages</h1>

    <table class="table table-minimal">
      <thead>
        <tr>
          <th>Body</th>
          <th><span class="pull-right">Sent</span></th>
        </tr>
      </thead>

      <tbody>
        <% @messages.each do |message| %>
          <tr>
            <td><%= message.body %></td>
            <td><span class="pull-right"><%= message.created_at %></span></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to 'New Message', new_merchant_message_path(@merchant), class: 'btn btn-primary', style: 'width: 100%;' %>
  </div>
</div>